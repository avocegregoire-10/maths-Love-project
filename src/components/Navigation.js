{/*import React from 'react';
import {NavLink} from 'react-router-dom';

const Navigation = () => {
    return (
        <div className='navigation'>
            <NavLink exact to = "/" activeClassName="nav-active">
                Home
            </NavLink>

            <NavLink exact to = "/course" activeClassName="nav-active">
                Courses
            </NavLink>

            <NavLink exact to = "/feature" activeClassName="nav-active">
                Features
            </NavLink>

            <NavLink exact to = "/blog" activeClassName="nav-active">
                Blog
            </NavLink>

            <NavLink exact to = "/page" activeClassName="nav-active">
                Pages
            </NavLink>

            <NavLink exact to = "/register" activeClassName="nav-active">
                Register
            </NavLink>
        </div>
    );
}

export default Navigation;*/}